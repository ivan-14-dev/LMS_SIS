"""App config for rattrapages."""

from django.apps import AppConfig


class RattrapagesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.rattrapages"
    verbose_name = "Rattrapages"
