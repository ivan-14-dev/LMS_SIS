"""App config for mobilite."""

from django.apps import AppConfig


class MobiliteConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.mobilite"
    verbose_name = "Mobilite"
