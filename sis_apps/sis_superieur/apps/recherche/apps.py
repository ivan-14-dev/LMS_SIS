"""App config for recherche."""

from django.apps import AppConfig


class RechercheConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.recherche"
    verbose_name = "Recherche"
