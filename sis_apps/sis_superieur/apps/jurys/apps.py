"""App config for jurys."""

from django.apps import AppConfig


class JurysConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.jurys"
    verbose_name = "Jurys"
