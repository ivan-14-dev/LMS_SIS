"""App config for releves."""
from django.apps import AppConfig


class RelevesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.releves"
    verbose_name = "Releves"
