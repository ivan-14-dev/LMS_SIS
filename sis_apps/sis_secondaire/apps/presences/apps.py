"""App config for presences."""

from django.apps import AppConfig


class PresencesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.presences"
    verbose_name = "Presences"
