"""App config for infirmerie."""

from django.apps import AppConfig


class InfirmerieConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.infirmerie"
    verbose_name = "Infirmerie"
