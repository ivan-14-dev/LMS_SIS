"""App config for conseil_classe."""
from django.apps import AppConfig


class ConseilClasseConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.conseil_classe"
    verbose_name = "ConseilClasse"
