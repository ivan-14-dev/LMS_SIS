"""App config for salles."""

from django.apps import AppConfig


class SallesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.salles"
    verbose_name = "Salles"
