"""App config for internat."""

from django.apps import AppConfig


class InternatConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.internat"
    verbose_name = "Internat"
