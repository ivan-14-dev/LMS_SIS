"""App config for portail_parent."""
from django.apps import AppConfig


class PortailParentConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.portail_parent"
    verbose_name = "PortailParent"
